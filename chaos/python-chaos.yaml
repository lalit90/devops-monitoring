apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: python-pod-kill
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      "app": "python-service"
  duration: "2m"
